class speed {
  
  constructor(speedLevel){
    this.speedLevel = speedLevel;
    speedLevel = 0;
  }
  
  get speedLevel(){
    return defenseLevel;
  }
  
  set speedLevel(newSpeedLevel){
      speedLevel = newSpeedLevel;
  }
}